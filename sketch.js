function setup() {
	// create a place to draw
	createCanvas(640, 360);
}

function draw() {
	// clear the background
	background(0, 0, 0);

	// set a fill color
	fill(255, 255, 255);

	// set a stroke color
	stroke(255, 0, 0);

	// draw a circle
	ellipse(320, 180, 100, 100);
}
